---
name: <skill-name>
description: <一句话说明这个 skill 做什么 + 在什么场景下触发使用>
---

# 目标
说明该 skill 要解决的问题和边界，不写泛泛背景。

# 输入
- 列出执行该 skill 所需的最小输入信息。
- 对缺失输入时的处理方式给出明确规则。

# 输出
- 定义可交付结果的格式与质量标准。
- 说明输出中必须包含的关键内容。

# 执行步骤
1. 澄清目标与约束，确认输入是否完整。
2. 按固定流程执行核心任务。
3. 自检结果，确保满足输出标准后再返回。

# 约束与注意事项
- 列出必须遵守的规则（安全、格式、工具限制、禁止项）。
- 若存在模型差异，仅在此处说明“需要查看 models/<provider>/SKILL.md”。

# 失败与降级
- 输入不足时如何最小化推进。
- 工具或资源不可用时的替代路径。
- 无法完成时的明确反馈格式。

# 快速检查清单
- [ ] frontmatter 仅包含 `name` 和 `description`
- [ ] 步骤可执行且顺序清晰
- [ ] 输出标准可验证
- [ ] 约束具体，不含模糊描述

# 示例
## 示例 1：标准场景
输入：
- <示例输入>

输出：
- <示例输出结构>

## 示例 2：异常场景
输入：
- <缺失或冲突输入>

输出：
- <降级处理结果>
